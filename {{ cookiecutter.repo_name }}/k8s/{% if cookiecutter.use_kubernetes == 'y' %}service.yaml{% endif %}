apiVersion: v1
kind: Service
metadata:
  name: "{{ cookiecutter.repo_name }}"
spec:
  type: ClusterIP
  selector:
    app: "{{ cookiecutter.repo_name }}"
  ports:
    - port: 80
      targetPort: 8000
